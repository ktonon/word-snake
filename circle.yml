machine:
  environment:
    PATH: "$HOME/.asdf/bin:$HOME/.asdf/shims:$PATH"

dependencies:
  cache_directories:
    - ~/.asdf
    - ~/.mix
    - deps
    - _build
  pre:
    - if ! asdf | grep version; then git clone https://github.com/HashNuke/asdf.git ~/.asdf; fi
    - asdf plugin-list | grep erlang || asdf plugin-add erlang https://github.com/HashNuke/asdf-erlang.git
    - asdf plugin-list | grep elixir || asdf plugin-add elixir https://github.com/HashNuke/asdf-elixir.git
    - erlang_version=$(awk '/erlang/ { print $2 }' .tool-versions) && asdf install erlang ${erlang_version}
    - elixir_version=$(awk '/elixir/ { print $2 }' .tool-versions) && asdf install elixir ${elixir_version}
    - mix local.hex --force
    - mix local.rebar --force
    - mix deps.get
    - MIX_ENV=test mix deps.compile

test:
  override:
    - mix test && mix credo && npm test

# NOTE: Deploying on circleci takes too long because elm runs very slowly
#       (often more than 10 minutes)
#       Maybe because we are installing elm with npm, might be better to build
#       from source in a base docker image
# deployment:
#   staging:
#     branch: master
#     owner: ktonon
#     commands:
#       - sudo apt-get install python-dev zip
#       - sudo pip install --upgrade docker-compose==1.7.1
#       - npm install -g elm
#       - rel/build.sh:
#           timeout: 1800
#       - rel/deploy.sh
